- [ ] Update frontend `page.tsx` tab key mismatch (`schema` vs `schemas`) and render conditions.
- [ ] Widen `StageData` field types to avoid strict TS issues.
- [ ] Re-run frontend typecheck/build to confirm errors are resolved.

